package sec02.exam04;

public class ForFloatCounterExample {

	public static void main(String[] args) {
		
	
		for(float x=0.1f; x<=1.0f; x+=0.1f) {
			System.out.println(x);
			
			//9반 나오는 이유 : 
			//x에 0.1이 추가가 될때 정확히 0.1이 들어가는게 아니라 0.7부터는 x가 0.7보다 큰수가 되어버리는 것.
			//0.9000001값이 x<=1.0f;보다는 작기때문에 실행을 하였지만
			//그다음 돌때는 x가 1.0보다는 커지는 것이다. 그래서 x<=1.0f;가 false가 되어버리기때문에 반복에서 빠져 나오는 것임.
			//이와같은 현상이 생기는 이유는
			//float타입은 부동소수점 방식으로 저장하기때문에 0.1을 정확하게 표현 할 수가 없다.
			//그래서 0.1보다 큰 값이 x에 더하기가 되어서 9번만 반복하고 빠져나오게 되는것임.
			//결론 = for문을 작성할때는 가능하면 반복과 관련된 변수, 즉 초기화 식에 사용되는 변수(float x=0.1f;)의 타입은 '정수'타입으로 해주는 것이 좋다.
			

		}

	}
}